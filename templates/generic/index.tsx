import { FC } from "react";



export interface Props<T extends JSX.IntrinsicAttributes> {
    
};

export default function templatename<T extends JSX.IntrinsicAttributes>({
    
}: Props<T>) {

    

    return (
        <div>
            TemplateName generic component
        </div>
    );
}